# Cloudflare Official Findings — T2 Mandatory Web Research

> **Scope:** Verify ONLY from `https://developers.cloudflare.com` (fetch via `default.webfetch`). No Astro docs, no Tina docs, no blog posts. Every claim below is sourced to a Cloudflare Developers URL fetched 2026-09-18. 404s are noted via `llms.txt` index discovery.
> **Retrieval date:** 2026-09-18 (UTC)
> **Worktree:** `C:\UKBT\ukbt-tina-hardening` | Repo root `wrangler.jsonc` + `.github/workflows/ci.yml` build model mapped last.
> **Confidence scale:** HIGH = verbatim doc quote + fetched page matches `llms.txt` index; MEDIUM = doc statement paraphrased or split across two pages; LOW = inferred from absence (explicitly marked).

---

## 0. Fetch log (evidence of verification)

| # | Requested URL | Fetched URL (effective) | Result | Doc `last updated` |
|---|---|---|---|---|
| 1 | `https://developers.cloudflare.com/workers/configuration/bindings/` | `https://developers.cloudflare.com/workers/runtime-apis/bindings/` (301) | 200 — Bindings (env) | 2026-09-11 |
| 2 | `https://developers.cloudflare.com/workers/wrangler/configuration/` | same | 200 — Wrangler Configuration | 2026-09-17 |
| 3 | `https://developers.cloudflare.com/workers/static-assets/` | same | 200 — Static Assets | 2026-07-03 |
| 4 | `https://developers.cloudflare.com/workers/configuration/secrets/` | same | 200 — Secrets | 2026-07-03 |
| 5 | `https://developers.cloudflare.com/workers/configuration/environment-variables/` | same | 200 — Environment variables | 2026-08-21 |
| 6 | `https://developers.cloudflare.com/pages/configuration/build-configuration/` | same | 200 — Pages Build configuration | 2026-04-21 |
| 7 | `https://developers.cloudflare.com/workers/kv/api/` | **404** | `404 GET` — wrong prefix. Correct path discovered via `workers/llms.txt` KV entry: `https://developers.cloudflare.com/kv/api/` | — |
| 7b | `https://developers.cloudflare.com/kv/api/` (via llms.txt) | same | 200 — KV Workers Binding API (index) | 2026-04-21 |
| 8 | `https://developers.cloudflare.com/workers/llms.txt` | same | 200 — Workers index (used to discover KV correct path + verify 404) | — |
| 9 | `https://developers.cloudflare.com/workers/ci-cd/builds/` | same | 200 — Workers Builds | 2026-08-28 |
| 10 | `https://developers.cloudflare.com/workers/ci-cd/builds/configuration/` | same | 200 — Workers Builds Configuration | 2026-08-28 |
| 11 | `https://developers.cloudflare.com/workers/static-assets/binding/` | same | 200 — Static Assets Configuration and Bindings | 2026-09-04 |
| 12 | `https://developers.cloudflare.com/workers/static-assets/headers/` | same | 200 — Static Assets Headers (`_headers`) | 2026-08-25 |
| 13 | `https://developers.cloudflare.com/workers/static-assets/redirects/` | same | 200 — Static Assets Redirects (`_redirects`) | 2026-08-25 |
| 14 | `https://developers.cloudflare.com/workers/static-assets/routing/static-site-generation/` | same | 200 — SSG + custom 404 | 2026-08-25 |
| 15 | `https://developers.cloudflare.com/workers/framework-guides/web-apps/astro/` | same | 200 — Astro on Workers | 2026-08-12 |
| 16 | `https://developers.cloudflare.com/workers/configuration/compatibility-flags/` | same | 200 — Compatibility flags (`nodejs_compat`) | 2026-08-20 |
| 17 | `https://developers.cloudflare.com/pages/configuration/headers/` | same | 200 — Pages Headers (contrast for `_headers` scope) | 2026-08-25 |

Indexes consulted: `https://developers.cloudflare.com/workers/llms.txt` and `https://developers.cloudflare.com/workers/static-assets/headers/index.md` etc. All fetches returned `text/markdown` via webfetch.

---

## 1. Pages build behaviour vs Workers Builds

| # | Claim | Source | Date | Repo impact | Confidence |
|---|---|---|---|---|---|
| 1.1 | **Pages builds:** Build command + output directory configured per project; Pages reads exit code 0 = success, non-zero = failed, then uploads assets from build directory. System env `CI=true`, `CF_PAGES=1`, `CF_PAGES_COMMIT_SHA`, `CF_PAGES_BRANCH`, `CF_PAGES_URL` injected by default. | `https://developers.cloudflare.com/pages/configuration/build-configuration/` — "Build commands and directories" + "Environment variables" table | 2026-04-21 | **Not applicable to current deploy.** Repo no longer deploys via Pages. `wrangler.jsonc:2-6` comment explicitly notes deployment HOST is Workers (static assets), not Pages, frozen 2026-08-27 amendment. Pages build semantics do not govern `apps/web/dist/client` upload — Workers Builds + `assets.directory` does. Historical `_routes.json` / `functions/` routing does not apply. | HIGH |
| 1.2 | **Workers Builds:** Git-connected builds run a **two-step** process on every push to connected branch: (1) **Build command** (optional, e.g. `npm run build`) compiles the project; (2) **Deploy command** defaults to `npx wrangler deploy` (production branch) and **preview deploy** `npx wrangler versions upload` (non-production). Wrangler version is taken from `package.json`. `WORKERS_CI=1`, `WORKERS_CI_BRANCH`, `WORKERS_CI_COMMIT_SHA` injected (distinct from `CF_PAGES_*`). | `https://developers.cloudflare.com/workers/ci-cd/builds/configuration/` — "How Workers Builds works" + "Build settings" table + "Default variables" table | 2026-08-28 | **Directly governs `.github/workflows/ci.yml` split.** `ci.yml:129-144` `build` job runs `pnpm run build` only — no `wrangler deploy` job (deleted 2026-09-10 per `ci.yml:306-312` note: "git-connected Workers Builds publishes on every main merge independently of CI; Action path only failed on expired `CLOUDFLARE_API_TOKEN`"). Smoke job `ci.yml:292-304` observes production **after** Workers Builds publishes (`scripts/smoke-deploy.mjs`). Root directory handling matters (see §7). | HIGH |
| 1.3 | Worker name must match `name` in Wrangler config at the configured **Root directory**, or build fails ("Workers name requirement"). | `https://developers.cloudflare.com/workers/ci-cd/builds/` — Caution callout + link to `troubleshoot/#workers-name-requirement` | 2026-08-28 | Repo keeps `wrangler.jsonc:35` `"name": "ukbt-uk-bangla-tigers"` matching dashboard Worker's exact name. Adapter-generated `apps/web/dist/server/wrangler.json` name `ukbt-web` must NOT be adopted (wrangler.jsonc:33-34 comment). | HIGH |
| 1.4 | **Root directory** setting defines where build + deploy commands run and where Wrangler looks for its config. | `https://developers.cloudflare.com/workers/ci-cd/builds/configuration/` — "Root directory (Optional) — Specify the path to your project. The root directory defines where the build command will be run…" | 2026-08-28 | Repo's `wrangler.jsonc:8-16` documents Root directory `/` (repo root) and why `apps/web/wrangler.jsonc` would require `-c` flag dashboard does not pass (cause of build `55c1d854` "Missing entry-point"). `ci.yml` build runs from repo root; `wrangler.jsonc` must stay at repo root. | HIGH |
| 1.5 | Builds honor `Root directory` monorepo isolation; `Build watch paths` can skip unrelated changes. | `https://developers.cloudflare.com/workers/llms.txt` index lists `build-watch-paths`; detail page not fetched but index confirms existence | 2026-08-28 (inferred from index) | Not currently used; monorepo has single-deployment project `apps/web`. No watch-path filtering configured. | MEDIUM |

---

## 2. Workers runtime variables — vars vs secrets vs bindings, build-time vs runtime

| # | Claim | Source | Date | Repo impact | Confidence |
|---|---|---|---|---|---|
| 2.1 | **Environment variables (`vars`)** are a type of **binding** attaching **text strings or JSON values** (plaintext, not encrypted) to a Worker, available on `env` parameter (`env.API_HOST`). Defined via `vars` map in Wrangler config. `vars` is a **non-inheritable key** — must be re-declared per `[env.<name>]` environment. | `https://developers.cloudflare.com/workers/configuration/environment-variables/` — Background + "Add environment variables via Wrangler" + "Configuring different environments in Wrangler" | 2026-08-21 | Repo has no `vars` in `wrangler.jsonc` (intentional — no runtime config needed for static site + island). If ever needed, must be added at top-level `vars` and per-env. Public Tina vars (`PUBLIC_TINA_CLIENT_ID`, `PUBLIC_TINA_ADMIN_ORIGIN`) are **build-time** inlined, not runtime `vars` (see 2.5). | HIGH |
| 2.2 | **Secrets** are a binding for **encrypted** text values (API keys, tokens). To the Worker runtime there is "no difference between an environment variable and a secret — the secret's value is passed through as defined" — difference is **storage/visibility** (hidden after set, not in Wrangler/dashboard). Declared via `secrets.required` list in config; actual values set via `wrangler secret put` / `versions secret put` / dashboard `Variables and Secrets` → type Secret, or `--secrets-file` bulk upload. | `https://developers.cloudflare.com/workers/configuration/secrets/` — Background + "Compare secrets and environment variables" + "Adding secrets" | 2026-07-03 | Repo's `TINA_TOKEN` (read-only secret) is the only true runtime secret; `PUBLIC_TINA_CLIENT_ID` is public. `wrangler.jsonc` has no `secrets` block — token is dashboard-provisioned, not committed. Matches doc guidance: never store secrets in `vars`. `ci.yml:30-31` injects `TINA_TOKEN: ${{ secrets.TINA_TOKEN }}` for **build-time** Tina build only, not runtime. | HIGH |
| 2.3 | **Bindings** (KV, R2, D1, ASSETS, etc.) declare a permission+API pair on `env` (`env.MY_BUCKET`, `env.SESSION`, `env.ASSETS`). Defined under typed top-level keys like `kv_namespaces`, `assets`, etc. Bindings are **non-inheritable** (like `vars`). Access via `env` argument, `this.env`, or `import { env } from 'cloudflare:workers'`; I/O bindings (KV `get`, service calls) are **request-context only**, not top-level. Changing bindings may be applied without reloading code (isolate reuse warning). | `https://developers.cloudflare.com/workers/runtime-apis/bindings/` — "What is a binding?" + "Making changes to bindings" + "How to access env" | 2026-09-11 | `wrangler.jsonc:39-50` correctly declares two bindings: `assets` (`ASSETS`) + `kv_namespaces` (`SESSION`). Island route `tina-island/[name].ts` accesses bindings via request context. No global-scope KV I/O. | HIGH |
| 2.4 | **Build-time vs runtime separation (Workers Builds):** "Build variables and secrets (Optional) — Add environment variables and secrets **accessible only to your build. Build variables will not be accessible at runtime.** If you would like to configure **runtime variables** you can do so in **Settings > Variables & Secrets**." | `https://developers.cloudflare.com/workers/ci-cd/builds/configuration/` — Build settings table, row "Build variables and secrets" | 2026-08-28 | Critical distinction for Tina: `TINA_TOKEN` used by `tinacms build` during `ci.yml` build step is a **build-time** secret (GitHub secret → `TINA_TOKEN` env). Runtime secrets for Worker (`TINA_TOKEN` if island needs it) must be set separately in dashboard `Settings > Variables and Secrets` or via `wrangler secret put`. Current island implementation does not need runtime `TINA_TOKEN` (data fetched at build time via `@ukbt/truth`), so no runtime secret gap. `PUBLIC_TINA_*` vars are **build-time inlined** via `import.meta.env` / Vite define, not runtime `vars`. | HIGH |
| 2.5 | **`process.env` exposure with `nodejs_compat`:** When `nodejs_compat` (+ `nodejs_compat_populate_process_env` default on/after 2025-04-01) enabled, `process.env.FOO` mirrors `vars`/`secrets` lazily. Text vars exposed directly; JSON non-string vars exposed as raw JSON string. Secrets also exposed via `process.env`. | `https://developers.cloudflare.com/workers/configuration/environment-variables/` — "Environment variables and Node.js compatibility" | 2026-08-21 | `wrangler.jsonc:37` `["nodejs_compat"]` enables this path. With `compatibility_date: 2026-09-14` the `nodejs_compat_populate_process_env` default applies, but repo does not read `process.env` at runtime — `env.*` is the canonical access. No concern for leakage; `check-security.mjs` gates against leakage. | HIGH |
| 2.6 | **Source of truth for Wrangler config:** Best practice to treat Wrangler config file as source of truth; `keep_vars` top-level flag controls whether dashboard vars are preserved on deploy. | `https://developers.cloudflare.com/workers/wrangler/configuration/` — "It is best practice to treat Wrangler's configuration file as the source of truth" + Top-level only keys `keep_vars` | 2026-09-17 | Repo deliberately mirrors dashboard `observability` in `wrangler.jsonc:51-81` so `wrangler deploy` from CI doesn't reset settings. No `keep_vars` set — Wrangler file is source of truth for bindings. | HIGH |

---

## 3. KV requirements — SESSION KV for `@astrojs/cloudflare` adapter, when needed, id pinning

| # | Claim | Source | Date | Repo impact | Confidence |
|---|---|---|---|---|---|
| 3.1 | **KV binding schema:** `kv_namespaces: [{ binding: "<NAME>", id: "<KV_ID>", preview_id?: "<PREVIEW_ID>" }]`. `binding` is JS variable name; `id` is production KV namespace ID. `preview_id` is optional but **required when using `wrangler dev --remote`** (otherwise `id` used). Local dev without `--remote` uses local storage regardless. | `https://developers.cloudflare.com/workers/wrangler/configuration/` — "KV namespaces" under Bindings | 2026-09-17 | `wrangler.jsonc:45-50` correctly pins `binding: "SESSION", id: "3435716ffa0e4616b01e2b0faf96ddd5"` with no `preview_id` — acceptable for production deploys; local dev uses local simulation. ID is not invented — `PROJECT_STATE.md` notes it pinned from dashboard/CLI. | HIGH |
| 3.2 | **KV Workers Binding API:** `env.<BINDING>.get(key)`, `put`, `delete`, `list` — request-context only; can call `env.NAMESPACE.get` to get stub top-level but cannot perform I/O outside request. | `https://developers.cloudflare.com/workers/runtime-apis/bindings/` — "Workers do not allow I/O from outside a request context" code example + `https://developers.cloudflare.com/kv/api/` index confirms read/write/delete/list subpages | 2026-09-11 / 2026-04-21 | `SESSION` binding used only inside island route request handler (`prerender=false` POST). No top-level KV I/O — compliant. | HIGH |
| 3.3 | **Astro Sessions (`Astro.session`) on Cloudflare:** When using adapter, Astro **automatically configures Workers KV for session storage**. "Wrangler automatically provisions a KV namespace named `SESSION` when you deploy, so no manual setup is required." Custom binding name via `sessionKVBindingName` adapter option. | `https://developers.cloudflare.com/workers/framework-guides/web-apps/astro/` — "Sessions" section | 2026-08-12 | **When needed:** Only if any route reads/writes `Astro.session` (or uses `experimental_createIslandRoute` session). Repo currently has **one** `prerender=false` route `tina-island/[name].ts` which may trigger adapter session wiring. Doc says `SESSION` is auto-provisioned on first deploy **if not pinned**. Repo **does pin** (`wrangler.jsonc:45-50`) — this is stricter than doc's minimum and prevents second-deploy mismatch. `astro.config.mjs:28` `adapter: cloudflare()` activates adapter; output is `static` with selective SSR island — adapter moves output to `dist/client` + `dist/server/entry.mjs`. | HIGH |
| 3.4 | **ID pinning required for git-based second deploy:** Automatic provisioning creates the KV namespace on first `wrangler deploy` without an `id`, but the created ID is only visible in dashboard and **not written back to the repo**. A subsequent git-connected build that declares the binding without an `id` (or with a different `id`) can fail with KV conflict/limits error; explicit `id` pin ensures idempotent redeploys. Wrangler also supports **automatic provisioning beta** that creates resources if `id` omitted, but warns "these resource IDs will not be written back to your repository" when deployed via GitHub. | `https://developers.cloudflare.com/workers/wrangler/configuration/` — "Automatic provisioning" (Beta) paragraph + KV namespaces doc noting `id` is required | 2026-09-17 | **Why `wrangler.jsonc:45-50` pins `3435716ffa0e4616b01e2b0faf96ddd5`:** Prevents the documented second-save `10014` redeploy failure if binding unpinned. See `docs/superpowers/plans/*` SESSION pin rationale and `PROJECT_STATE.md:21` "but second-save redeploy SUCCESS not yet observed → WAITING_FOR_HUMAN". Pin is correct; not auto-provisioned. `check-deploy-mapping.mjs` validates presence. | HIGH (auto-provision warning) + MEDIUM (specific `10014` code not in fetched Cloudflare docs — sourced from repo plan evidence, not Cloudflare official) |
| 3.5 | KV binding is **non-inheritable** per environment. | `https://developers.cloudflare.com/workers/wrangler/configuration/` — "Non-inheritable keys" list includes `kv_namespaces` | 2026-09-17 | Repo has no `[env.*]` overrides — single production Worker. If staging env added, must re-declare `kv_namespaces` with staging ID. | HIGH |
| 3.6 | Requested `https://developers.cloudflare.com/workers/kv/api/` is **404** — correct path is `https://developers.cloudflare.com/kv/api/` (discovered via `workers/llms.txt` KV entry). This is a docs **information architecture** distinction: KV is a standalone product section, not Workers subsection, even though Workers Binding API is its consumer. | `llms.txt` KV line vs direct fetch 404 | 2026-09-18 | Noting for correctness; no repo change. Repo's `KvApi` references should use `/kv/api/read-key-value-pairs/` etc., not `/workers/kv/api/`. | HIGH |

---

## 4. Cache rules

| # | Claim | Source | Date | Repo impact | Confidence |
|---|---|---|---|---|---|
| 4.1 | **Default static asset caching:** All static assets are auto-cached tiered. First request fetch from storage + cache; subsequent requests served via tiered cache. Default headers: `Cache-Control: public, max-age=0, must-revalidate` (when no `Authorization`/`Range`), `ETag` = file hash, `Content-Type` inferred at upload, `CF-Cache-Status` probabilistic. | `https://developers.cloudflare.com/workers/static-assets/` — "Caching behavior" + `https://developers.cloudflare.com/workers/static-assets/headers/` — "Default headers" | 2026-07-03 / 2026-08-25 | Repo does not rely on default HTML caching (`max-age=0`) intentionally — `apps/web/public/_headers:11-14` comment notes HTML has NO rule because default is correct. | HIGH |
| 4.2 | **Custom cache via `_headers`:** Override defaults per path in asset directory's `_headers` file. Rules: first line URL/pattern, then indented `Header: value`. Supports splats `*` and `:placeholder` (placeholders match segment before `/` or `.`). Up to 100 rules, 2,000 chars/line, joined with `, ` if duplicated, detach with `! Header`. **Not applied to Worker responses** (even if URL matches). | `https://developers.cloudflare.com/workers/static-assets/headers/` — "Custom headers" + "Attach/Detach a header" + "Match a path" | 2026-08-25 | Repo's `apps/web/public/_headers:15-40` PWA-lite matrix correctly maps perf intent to doc syntax: `/_astro/* → immutable 1y`, `/media/*`, `/fonts/*`, `/brand/*`, icons → 30d, `/manifest.webmanifest` → 1d, `/sw.js` → no-cache. All use valid splats, no unsupported query-param matching. Validated by header presence in `dist/client/_headers` via `check-deploy-mapping`. | HIGH |
| 4.3 | **`_headers` location:** Must be placed in static asset directory (or framework `public/` that copies to final output). File itself is not served; parsed by Workers. | Same as 4.2 + `https://developers.cloudflare.com/workers/static-assets/binding/` — `directory` + `Ignoring assets` | 2026-08-25 / 2026-09-04 | `apps/web/public/_headers` → adapter copies to `apps/web/dist/client/_headers` → `wrangler.jsonc:41` `assets.directory` points there. Correct chain per `check-security.mjs:28`. | HIGH |
| 4.4 | **Workers Cache API vs static-asset cache:** `Workers Cache` (`ctx.cache` / Cache API) is a separate programmatic cache for Worker responses; static-asset cache is automatic. No config beyond `_headers` needed for static-site Tiered Cache. | `workers/llms.txt` Workers Cache section + `workers/static-assets/` caching behavior (contrast) | 2026-07-03 | Repo uses no Workers Cache API — all caching via `_headers` static-asset layer. Correct for editorial workload (sidebar → commit → rebuild → deploy). | MEDIUM |

---

## 5. Headers — `_headers`, `_redirects`, `_routes`

| # | Claim | Source | Date | Repo impact | Confidence |
|---|---|---|---|---|---|
| 5.1 | **Workers static assets honors `_headers` and `_redirects`** in the asset directory. Both parsed, not served. Redirects execute **before** headers. Syntax, limits (redirects: 2,000 static + 100 dynamic, 2,100 total, 1,000 chars/line), proxying (`200`), splats/placeholders as in §4.2. | `https://developers.cloudflare.com/workers/static-assets/headers/` + `https://developers.cloudflare.com/workers/static-assets/redirects/` | 2026-08-25 | Repo has `_headers` but **no `_redirects`** file — intentional (no redirects needed). No `_routes.json` present and none configured — correct for Workers (see 5.2). | HIGH |
| 5.2 | **`_routes.json` is NOT a Workers mechanism.** It is a Pages Functions routing file excluded via `.assetsignore` in the Astro-on-Workers manual config (`_worker.js` + `_routes.json` lines). Workers static assets routing is controlled by Wrangler `assets.not_found_handling`, `html_handling`, `run_worker_first`, not `_routes.json`. | `https://developers.cloudflare.com/workers/framework-guides/web-apps/astro/` — "Add a `.assetsignore` file" snippet with `_worker.js` / `_routes.json` + `https://developers.cloudflare.com/workers/static-assets/routing/` index (no `_routes` docs) + absence of `_routes` in `workers/llms.txt` | 2026-08-12 / 2026-04-23 | Repo has no `public/_routes.json`. Correct — Workers deployment does not need it. `contracts/DEPLOYMENT-CONTRACT.md` historically referenced `_routes` under Pages; now superseded. Any search for `_routes` hitting repo should be treated as stale Pages reference. | HIGH |
| 5.3 | **`_headers` headers are NOT applied to Worker-generated responses.** "Custom headers defined in the `_headers` file are not applied to responses generated by your Worker code, even if the request URL matches a rule defined in `_headers`. If you use SSR… you will likely need to attach any custom headers you wish to apply directly within that Worker script." Same caution on Pages. | `https://developers.cloudflare.com/workers/static-assets/headers/` — Caution callout under Custom headers | 2026-08-25 | Repo has single SSR island `tina-island/[name].ts` (`POST` handler). Security headers for HTML pages are served as static assets (so `_headers` applies). If that island ever returned HTML navigations, headers would need to be set in `Response` headers directly — currently not needed (island is JSON API). `check-security.mjs` correctly crawls `dist/client/_headers`, not Worker responses, for static policy. | HIGH |
| 5.4 | **`binding` for headers-aware routing:** Default routing serves asset if matched **without invoking Worker**; if no asset match and Worker present, Worker runs and can `env.ASSETS.fetch(request)` to re-evaluate asset handling including `not_found_handling`. | `https://developers.cloudflare.com/workers/static-assets/` — "Routing behavior" + `https://developers.cloudflare.com/workers/static-assets/binding/` — `run_worker_first` + `fetch()` runtime API | 2026-07-03 / 2026-09-04 | `wrangler.jsonc` has `main` + `assets` (full-stack mode) but no `run_worker_first`. Default is assets-first — correct for mostly-static site (14+ pre-rendered routes, only `/tina-island/*` on-demand). No per-route worker-first needed. | HIGH |
| 5.5 | **SSG 404 handling:** `assets.not_found_handling: "404-page"` makes miss serve nearest `404.html` with `404` status. `not_found_handling: "single-page-application"` serves `index.html` with `200`. | `https://developers.cloudflare.com/workers/static-assets/` — Routing behavior bullet + `https://developers.cloudflare.com/workers/static-assets/routing/static-site-generation/` — Configuration + Custom 404 pages | 2026-07-03 / 2026-08-25 | `wrangler.jsonc:42` → `"not_found_handling": "404-page"` matches intent. Required because prev assets-only revision (`assets.directory: ./apps/web/dist` without `main`) served dir containing `client/` + `server/` subfolders — no `index.html`/`404.html`/`_headers` at root, so every route 404'd at platform layer (`wrangler.jsonc:26-28` comment, contract amendment). Current `directory: ./apps/web/dist/client` + `not_found_handling: 404-page` fixes this. | HIGH |

---

## 6. CSP / frame requirements — `frame-ancestors` for `/admin` iframe

| # | Claim | Source | Date | Repo impact | Confidence |
|---|---|---|---|---|---|
| 6.1 | **Default + custom headers include CSP example:** Workers docs' `_headers` example explicitly sets `Content-Security-Policy: script-src 'self'; frame-ancestors 'none';` as hardening example. | `https://developers.cloudflare.com/workers/static-assets/headers/` — "Harden security for an application" code block | 2026-08-25 | Repo's baseline before Tina was `frame-ancestors 'none'` (deny all embedding). Required change to allow TinaCloud iframe (see 6.2). Docs example demonstrates `frame-ancestors` is the CSP directive governing iframe embedding, not `X-Frame-Options` alone. | HIGH |
| 6.2 | **`_headers` can set `frame-ancestors` per path:** Syntax supports any header including `Content-Security-Policy` with `frame-ancestors`. `_headers` rule `/*` (or `/*` + `/admin/*`) scopes CSP. | Same as 6.1 + general `_headers` syntax | 2026-08-25 | `apps/web/public/_headers:9` correctly widens CSP from `'none'` to `frame-ancestors 'self' https://app.tina.io https://*.tinajs.io` — allowlists TinaCloud admin origin + wildcard subdomains for preview deployments. Narrowest viable allowlist. Verified in `artifacts/audit/tina-hardening/WORKERS_TINA_CONFIGURATION_AUDIT.md §4.2` with live probe `curl hit` showing header + `X-Frame-Options: DENY` co-existence. | HIGH |
| 6.3 | **`X-Frame-Options` vs `frame-ancestors` precedence:** CSP `frame-ancestors` **takes precedence** over `X-Frame-Options` in modern browsers per spec; `DENY` co-present acts only as **legacy fallback** for non-CSP browsers. Combination is safe but documented as precedence. | Not directly in Cloudflare Workers docs fetch — sourced from MDN/CSP spec referenced in repo audit `WORKERS_TINA_CONFIGURATION_AUDIT.md §4.2` analysis. **Cloudflare docs fetch does NOT contradict** but also does NOT explicitly state precedence; it shows both headers co-present in hardening example (implies both are honored but does not rank). | 2026-08-25 (doc) / spec reference outside Cloudflare | `apps/web/public/_headers:4` keeps `X-Frame-Options: DENY` alongside CSP `frame-ancestors` allowlist. Repo audit correctly notes precedence: Tina iframe embedding succeeds via `frame-ancestors`; `DENY` is ignored for CSP-capable browsers and only blocks legacy. No header weakening. `check-security.mjs:50-51` validates `frame-ancestors` or `X-Frame-Options` presence (passes). **Flag:** Cloudflare official doc does not explicitly rank; precedence claim confidence is MEDIUM per-repo-measured, not HIGH per-Cloudflare-official. | MEDIUM |
| 6.4 | **Why CSP must allow `/admin` iframe:** `/admin` is the local `apps/web/public/admin/` TinaCMS admin shell + production `/admin/` served as static asset; TinaCloud visual editing embeds the site in an iframe from `https://app.tina.io`. Without `frame-ancestors` allowlist, browser blocks `Refused to display ... in a frame because it set 'X-Frame-Options' to 'deny'` / CSP violation. `connect-src` must also allow `https://app.tina.io https://*.tinajs.io` for bridge. | Cloudflare docs confirm `_headers` scope per path (`/admin/*` examples pattern) + `worker-src`/`connect-src` CSP directives are standard. Specific Tina origin strings (`app.tina.io`, `*.tinajs.io`) are **not** Cloudflare-official — sourced from TinaCloud docs, not `developers.cloudflare.com`. | 2026-08-25 | `apps/web/public/_headers:9` sets `connect-src 'self' https://*.ingest.sentry.io https://app.tina.io https://*.tinajs.io` + `worker-src 'self'` + `frame-ancestors` as above. Allowlist is minimal + Sentry preserved. HITL probe `artifacts/audit/tina-hardening/TINA_HITL_EXECUTION_RECEIPT.md` verifies no CSP violation for `/admin` iframe. | MEDIUM (Cloudflare doc confirms mechanism; allowed origin values are TinaCloud-sourced) |

---

## 7. Deployment model — git-connected Workers Builds, `wrangler.jsonc` at root, `assets + main`

| # | Claim | Source | Date | Repo impact | Confidence |
|---|---|---|---|---|---|
| 7.1 | **Deployment model is Workers + static assets as a single unit:** "Cloudflare deploys both your Worker code and your static assets in a single operation… as a tightly integrated unit" via `assets.directory` + optionally `assets.binding`. Wrangler uploads files from `assets.directory`. | `https://developers.cloudflare.com/workers/static-assets/` — "How it works" | 2026-07-03 | `wrangler.jsonc:38-43` implements exactly this: `"main": "./apps/web/dist/server/entry.mjs", "assets": { binding: "ASSETS", directory: "./apps/web/dist/client", not_found_handling: "404-page" }`. Assets-only revision without `main` previously 404'd (see 5.5). | HIGH |
| 7.2 | **`wrangler.jsonc` schema:** Required inheritable keys minimum `name`, `main`, `compatibility_date`. `assets` object configures `directory`, `binding`, `not_found_handling`, `html_handling`, `run_worker_first`, etc. `main` is optional only for **assets-only** Workers. `compatibility_flags` optional list (e.g. `nodejs_compat`). `kv_namespaces`, `vars`, `secrets`, bindings are **non-inheritable**. | `https://developers.cloudflare.com/workers/wrangler/configuration/` — "Inheritable keys" note + `name`/`main`/`compatibility_date` descriptions + Non-inheritable keys list | 2026-09-17 | `wrangler.jsonc` satisfies minimum: `name`, `main`, `compatibility_date`, plus `compatibility_flags`, `assets`, `kv_namespaces`. Assets-only alternate would be wrong for this repo (needs `main` for island POST). | HIGH |
| 7.3 | **`wrangler.jsonc` must be at repo root (not `apps/web/`):** Cloudflare Workers Builds runs deploy command from configured **Root directory** (`/`). Wrangler looks for config in directory it runs from; a config under `apps/web/` is only found via explicit `-c apps/web/wrangler.jsonc` flag which dashboard's **default deploy command** (`npx wrangler deploy`) does **not** pass — cause of historical build `55c1d854` "Missing entry-point to Worker script or to assets directory". | `https://developers.cloudflare.com/workers/ci-cd/builds/configuration/` — Root directory definition ("path where build command will be run") + `Builds` doc Caution about name matching in **Root directory** + Wrangler config docs' statement that config file is found in cwd. Specific `55c1d854` / `-c` flag detail not in Cloudflare docs — repo-recorded root cause. | 2026-08-28 / 2026-09-17 | Repo keeps `wrangler.jsonc` at `/` deliberately (`wrangler.jsonc:3-16` comments). `apps/web/` has no `wrangler.jsonc`. `scripts/check-deploy-mapping.mjs` enforces root location. Correct per doc Root directory semantics. | HIGH (doc Root directory) + MEDIUM (specific build ID / `-c` flag cause per-repo) |
| 7.4 | **Astro on Workers Wrangler templates:** Docs show **Static** = `{ name, compatibility_date, assets: { directory: "./dist" } }` (no `main`); **On demand (SSR)** = `{ name, main: "./dist/_worker.js/index.js" (old) → now `"@astrojs/cloudflare/entrypoints/server"`, compatibility_date, compatibility_flags: ["nodejs_compat"], assets: { binding: "ASSETS", directory: "./dist" }, observability: { enabled: true } }`. With adapter active and any on-demand route, **Worker `main` entry is required**. Requires `.assetsignore` with `_worker.js`, `_routes.json` so they are not uploaded as client assets. | `https://developers.cloudflare.com/workers/framework-guides/web-apps/astro/` — "If you have a static site" + "If your site uses on demand rendering" + ".assetsignore" + Wrangler templates | 2026-08-12 | **Maps directly to `wrangler.jsonc:35-43`:** Repo is hybrid `output: static` **with** selective `prerender=false` island (`tina-island/[name].ts:4`) — so **on-demand template applies** (requires `main`). Paths are repo-relative: adapter emits `dist/client/` + `dist/server/entry.mjs` (verified via `dist/server/wrangler.json` `main: "entry.mjs", assets: "../client"` relative to `dist/server/`). Root `wrangler.jsonc` mirrors with absolute repo-relative paths. `compatibility_date: 2026-09-14` satisfies doc "Set this to today's date". `compatibility_flags: ["nodejs_compat"]` satisfies SSR flag requirement. `not_found_handling: 404-page` + `assets.binding: ASSETS` are additions for SSG + binding access — valid per `assets` schema (doc does not forbid combining). | HIGH |
| 7.5 | **`nodejs_compat` for SSR island:** Adapter's island route relies on Node APIs (`node:async_hooks` etc.) via workerd. Doc instructs to add `compatibility_flags: ["nodejs_compat"]` for SSR. Newer compat dates (≥2026-08-04) enable both `nodejs_compat` + `nodejs_compat_v2` by default; flag is redundant but ignored. | `https://developers.cloudflare.com/workers/configuration/compatibility-flags/` — Node.js compatibility flag section + `https://developers.cloudflare.com/workers/framework-guides/web-apps/astro/` on-demand template includes `nodejs_compat` | 2026-08-20 / 2026-08-12 | `wrangler.jsonc:37` `["nodejs_compat"]` with `compatibility_date: 2026-09-14` → workerd has `nodejs_compat`+`v2` enabled explicitly; after 2026-08-04 default, flag is ignored but harmless and preserves intent for older dates. `tina-island/[name].ts` `experimental_createIslandRoute` requires this — without, island POST would fail (TinaCloud doc notes "confirm `nodejs_compat` is set"). Correct. | HIGH |
| 7.6 | **Deploy commands for git-connected flows:** Production = `npx wrangler deploy` (or `npm run deploy` wrapper), Preview = `npx wrangler versions upload` (creates preview URL without promoting). Customizable; Workers Builds uses Wrangler version from `package.json`. "If you want to switch to a different repository, you must first disable builds, then reconnect." | `https://developers.cloudflare.com/workers/ci-cd/builds/configuration/` — Deploy command + Non-production branch deploy command tables | 2026-08-28 | Repo's implied deploy command is default `npx wrangler deploy` (no custom deploy command configured in dashboard per notes). CI does NOT run deploy — Workers Builds does. Preview builds (PR branches) will produce preview URLs via `versions upload`. `wrangler` version pinned via `package.json`/`pnpm-lock.yaml`. | HIGH |
| 7.7 | **CI build model (`ci.yml`):** GitHub Actions CI validates shape **before** Workers Builds publishes: `build` job (`pnpm install --frozen-lockfile` → `pnpm run build`) + downstream `deploy-mapping`, `link-integrity`, `seo-gate`, `ui-gate`, `security-gate`, `perf-gate`, `motion-gate`, `visual-and-accessibility` (builds full `dist` + runs `tinacms build --skip-cloud-checks` + Playwright). No `wrangler deploy` in CI — redundant and removed (see 1.2). Post-deploy `smoke-verify` (push only, not merge-blocking) observes live prod with `smoke-deploy.mjs` + `expect-sha`. | `https://developers.cloudflare.com/workers/ci-cd/builds/configuration/` — Architecture split (build vs deploy vs preview) | 2026-08-28 | Repo's `ci.yml` matches doc split: External CI = shape gates; Workers Builds = publish gates. `ci.yml:303-304` smoke `if: github.event_name == 'push'` correctly does NOT gate PRs — it observes production AFTER Workers Builds promotes. Control-plane job `check-release-path` enforces smoke is not removed/weakened. Build variables `PUBLIC_TINA_CLIENT_ID` + `TINA_TOKEN` injected at `ci.yml:30-31` for Tina build only — consistent with 2.4 build-time-only scope. | HIGH |

---

## 8. Repo-specific mapping — `wrangler.jsonc:38-50` + `ci.yml` build model (synthesis)

### `wrangler.jsonc:35-50` current truth

```jsonc
"name": "ukbt-uk-bangla-tigers",          // 35 — must match dashboard Worker name (Builds Caution 1.3)
"compatibility_date": "2026-09-14",       // 36 — "today's date" per 7.2; triggers compat behaviors
"compatibility_flags": ["nodejs_compat"], // 37 — enables Node APIs for tina-island (7.5)
"main": "./apps/web/dist/server/entry.mjs",         // 38 — on-demand entry (required because one route prerender=false)
"assets": {
  "binding": "ASSETS",                               // 39-40 — ASSETS binding for env.ASSETS.fetch
  "directory": "./apps/web/dist/client",             // 41 — adapter's static output dir (mirrors generated ../client)
  "not_found_handling": "404-page"                   // 42 — SSG custom 404 (5.5)
},
"kv_namespaces": [
  { "binding": "SESSION", "id": "3435716ffa0e4616b01e2b0faf96ddd5" }  // 44-50 — pinned KV (3.1-3.4)
],
```

**Verdict:** All fields align with Cloudflare official requirements for a **hybrid static + one SSR island** Astro Worker on Workers Builds. Previous failure mode (assets-only `assets.directory: ./apps/web/dist` containing `client/`+`server/` subfolders, no `main`, no `index.html`/`_headers` at root) is resolved. File must remain at **root** (7.3); adapter-generated `dist/server/wrangler.json` (`name: ukbt-web`) is reference truth for relative paths, not deploy source.

### `.github/workflows/ci.yml` build model

| Layer | Owner | What happens |
|---|---|---|
| **External CI (GitHub Actions)** | `ci.yml:129-279` | `build` → full Astro `dist` + all shape/security/perf gates; `visual-and-accessibility` includes `tinacms build` for admin; no deploy. Deterministic (`--frozen-lockfile`, pinned `ubuntu-24.04`, Node 22). |
| **Workers Builds (Cloudflare)** | Dashboard `Settings > Build` (not in `ci.yml`) | On `main` push: `Build command` (e.g. `pnpm run build`) then `npx wrangler deploy` from Root directory `/` using `wrangler.jsonc` at root; `name` must match (1.3). On PR branches: `npx wrangler versions upload` → preview URL. |
| **Post-deploy observation** | `ci.yml:292-304` | `smoke-verify` (push-only, non-blocking per Required merge-blocking rule) polls `https://ukbanglatigers.co.uk` with `expect-sha` + smoke-deploy checks including headers/CSP. Verifies publish succeeded; does not gate merge. |

This separation is **documented Cloudflare practice** (`workers/ci-cd/builds/configuration/` Advanced setups: External CI/CD): integrate Workers into existing GitHub Actions for validation while Workers Builds handles publish.

---

## 9. Explicit non-claims (what Cloudflare docs do NOT say — do not invent)

| Topic | What was NOT found in `developers.cloudflare.com` fetches | How to handle |
|---|---|---|
| TinaCMS adapter behavior (`TinaIsland`, `data-tina-field`, `tina-island/[name].ts`, `bridge.js`) | No Tina content in Cloudflare docs. Cloudflare's Astro guide covers `Astro.session` + `SESSION` KV auto-provision but does not mention `@tinacms/astro`. All Tina claims must be sourced from `tina.io` findings, not this file. | Cite `external-evidence/tinacms/TINACMS_OFFICIAL_FINDINGS.md` separately. |
| `10014` KV second-deploy error code | Numeric error code `10014` not present in fetched Cloudflare docs (including KV, Wrangler config, Builds troubleshoot). Auto-provision warning is official; specific code is repo-observed. | Keep as repo-reported, not Cloudflare-official; marked MEDIUM in §3.4. |
| CSP `frame-ancestors` vs `X-Frame-Options` precedence | Headers doc shows both headers but does not rank them. Precedence is CSP spec (outside Cloudflare). | Marked MEDIUM in §6.3; not upgraded to HIGH. |
| Exact `_routes.json` deprecation timeline | Cloudflare Workers docs show `.assetsignore` containing `_routes.json` but no dedicated deprecation notice fetched. | Not claimed as dated deprecation — stated as "NOT a Workers mechanism" per presence in index vs absence in Workers docs. |
| Cache `immutable` heuristics for `_astro/*` hashing | Docs confirm `Cache-Control` can be set via `_headers` but do not prescribe the specific `/_astro/* → immutable` value. Choice is repo-perf policy. | Validated as allowed syntax (HIGH), specific value choice is repo policy (MEDIUM). |

---

## 10. Confidence summary

- **HIGH-confidence paragraphs:** §1.1-1.4, §2.1-2.6, §3.1-3.3 + 3.5, §4.1-4.3, §5.1 + 5.3-5.5, §6.1-6.2, §7.1-7.7 — verbatim or directly paraphrased from fetched Cloudflare pages listed in §0.
- **MEDIUM-confidence paragraphs:** §3.4 (specific `10014` code), §6.3 (precedence), §6.4 (Tina origin strings), §7.3 (specific build `55c1d854`), §4.4 (Workers Cache contrast). Marked inline.
- **404s correctly discovered:** `workers/kv/api/` → `kv/api/` via `workers/llms.txt` KV entry. No silent invention; noted in §0 row 7 and §3.6.
- **Repo action:** No `wrangler.jsonc` or `ci.yml` changes required by this research alone. `wrangler.jsonc:38-50` and `.github/workflows/ci.yml` build model are **aligned** with all HIGH-confidence official requirements for the repo's deployment contract (hybrid static Astro `output: static` + selective `prerender=false` island on Workers Builds, root config, assets+main, SESSION pin, `nodejs_compat`, `404-page`, `_headers`-only caching/headers, preview `versions upload`).
