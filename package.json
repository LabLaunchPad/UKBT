{
  "name": "ukbt-uk-bangla-tigers",
  "private": true,
  "version": "0.0.0",
  "description": "UK Bangla Tigers production website — monorepo root. See contracts/REPOSITORY-CONTRACT.md.",
  "engines": {
    "node": ">=22.22.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "overrides": {
      "sharp": "^0.35.4",
      "qs": "^6.16.0"
    }
  },
  "scripts": {
    "build": "tinacms build --skip-search-index && pnpm --filter @ukbt/truth tokens:build && pnpm --filter @ukbt/web build",
    "dev": "pnpm --filter ./apps/web dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "pnpm -r test",
    "test:unit": "pnpm --filter ./packages/truth test",
    "test:e2e": "pnpm --filter ./apps/web test:e2e",
    "tokens:build": "pnpm --filter ./packages/truth tokens:build",
    "check:deps": "node scripts/check-dependency-allowlist.mjs",
    "check:links": "node scripts/check-internal-links.mjs",
    "sitemap": "node scripts/generate-sitemap.mjs",
    "check:seo": "node scripts/check-seo.mjs",
    "check:ui": "node scripts/check-ui.mjs",
    "check:motion": "node scripts/check-motion.mjs",
    "check:security": "node scripts/check-security.mjs",
    "check:perf": "node scripts/check-perf.mjs",
    "check:deploy-mapping": "node scripts/check-deploy-mapping.mjs",
    "check:release-path": "node scripts/check-release-path.mjs",
    "check:content-trust": "node scripts/check-content-trust.mjs",
    "test:failure-injection": "node scripts/test-deploy-failure-injection.mjs && node scripts/test-perf-failure-injection.mjs && node scripts/test-seo-failure-injection.mjs && node scripts/test-smoke-failure-injection.mjs && node scripts/test-content-trust-injection.mjs && node scripts/test-security-failure-injection.mjs && node scripts/test-release-path-failure-injection.mjs",
    "check:control-plane": "node scripts/check-control-plane.mjs",
    "smoke:deploy": "node scripts/smoke-deploy.mjs",
    "check:governance-scaffold": "node scripts/scaffold-self-test.mjs",
    "deploy:verify": "pnpm run check:governance-scaffold && pnpm run check:control-plane && pnpm run check:deps && pnpm lint && pnpm run tokens:build && pnpm typecheck && pnpm test:unit && pnpm run build && pnpm run check:deploy-mapping && pnpm run check:release-path && pnpm run check:content-trust && pnpm run test:failure-injection && pnpm run check:links && pnpm run check:seo && pnpm run check:ui && pnpm run check:motion && pnpm run check:security && pnpm run check:perf"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.13",
    "@tinacms/cli": "^3.0.0",
    "tinacms": "^3.14.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.126.0"
  }
}
