---
// TournamentGrid — re-art-directed. One large event + editorial CTA.
// Not four cards. Clean typography, accent border, no decorative tags.
interface Tournament {
  name: string;
  when: string;
  where: string;
}
interface Props {
  mainEvent: Tournament;
  otherEvents: Tournament[];
}
const { mainEvent, otherEvents } = Astro.props;
---

<div class="ukbt-tournaments">
  <div class="ukbt-tournaments__main">
    <h2 class="ukbt-tournaments__title"><a class="ukbt-tournaments__item-name ukbt-tournaments__title-link" href="/tournaments/#upcoming">{mainEvent.name}</a></h2>
    <div class="ukbt-tournaments__meta">
      <span class="ukbt-tournaments__meta-when">{mainEvent.when}</span>
      <span class="ukbt-tournaments__sep" aria-hidden="true">&middot;</span>
      <span>{mainEvent.where}</span>
    </div>
  </div>
  {
    otherEvents.length > 0 && (
      <div class="ukbt-tournaments__others">
        <span class="ukbt-tournaments__eyebrow ukbt-eyebrow">Also Coming</span>
        <ul class="ukbt-tournaments__list">
          {otherEvents.map((t) => (
            <li class="ukbt-tournaments__item">
              <a class="ukbt-tournaments__item-name" href="/tournaments/#upcoming">{t.name}</a>
              <span class="ukbt-tournaments__item-meta">
                {t.when} &middot; {t.where}
              </span>
            </li>
          ))}
        </ul>
      </div>
    )
  }
</div>

<style>
  .ukbt-tournaments {
    display: flex;
    flex-direction: column;
    gap: var(--ukbt-space-gap-5);
  }
  .ukbt-tournaments__main {
    display: flex;
    flex-direction: column;
    gap: var(--ukbt-space-3);
    /* No top rule: on the homepage this grid sits directly under the
       SectionHeader's own accent rule — a second rule here rendered as
       a duplicated divider. This component is homepage-only, so no
       other page loses a divider. Tightened top pad binds the main
       event to that rule (Academy → Tournaments transition fix). */
    padding-top: var(--ukbt-space-4);
  }
  /* Eyebrow uses the shared .ukbt-eyebrow grammar (base.css); the local
     class remains as the component hook + test selector. */
  .ukbt-tournaments__title {
    font-family: var(--ukbt-font-family-heading);
    font-size: clamp(2rem, 1.6rem + 2vw, 2.75rem);
    font-weight: 700;
    line-height: 1.1;
    margin: 0;
    letter-spacing: -0.02em;
    text-wrap: balance;
  }
  /* Direction A gold-rule grammar (D4+D6): event names are native
     anchors to the existing /tournaments/#upcoming anchor. The main
     event reuses the item-name grammar and overrides colour/weight/
     offset to the persistent gold rule (non-text accent on light);
     list names wipe one in on hover, token-timed. */
  .ukbt-tournaments__item-name {
    color: inherit;
    text-decoration: underline;
    text-decoration-color: transparent;
    text-decoration-thickness: 2px;
    text-underline-offset: var(--ukbt-space-1);
    transition: text-decoration-color var(--ukbt-motion-duration-fast)
      var(--ukbt-motion-easing-standard);
  }
  .ukbt-tournaments__item-name:hover {
    text-decoration-color: var(--ukbt-color-brand-accent);
  }
  .ukbt-tournaments__title-link {
    text-decoration-color: var(--ukbt-color-brand-accent);
    text-decoration-thickness: 3px;
    text-underline-offset: var(--ukbt-space-2);
  }
  .ukbt-tournaments__meta {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: var(--ukbt-space-2);
    font-size: var(--ukbt-font-size-1);
    color: var(--ukbt-color-neutral-700);
  }
  .ukbt-tournaments__meta-when {
    font-family: var(--ukbt-font-family-heading);
    font-weight: 600;
    color: var(--ukbt-color-brand-primary);
  }
  .ukbt-tournaments__sep {
    color: var(--ukbt-color-brand-primary);
  }
  .ukbt-tournaments__others {
    display: flex;
    flex-direction: column;
    gap: var(--ukbt-space-4);
  }
  .ukbt-tournaments__list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: var(--ukbt-space-4);
  }
  .ukbt-tournaments__item {
    display: flex;
    flex-direction: column;
    gap: var(--ukbt-space-1);
    padding-top: var(--ukbt-space-4);
    border-top: 1px solid var(--ukbt-color-border-subtle);
  }
  .ukbt-tournaments__item-name {
    font-family: var(--ukbt-font-family-heading);
    font-size: var(--ukbt-font-size-1);
    font-weight: 600;
  }
  .ukbt-tournaments__item-meta {
    font-size: var(--ukbt-font-size-0);
    color: var(--ukbt-color-neutral-700);
  }

  @media screen and (max-width: 767px) {
    .ukbt-tournaments__meta {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--ukbt-space-1);
    }
    .ukbt-tournaments__sep {
      display: none;
    }
  }
</style>
