# TINACMS Architect Report — Production Alignment

> **Date:** 2026-09-18 (UTC) · Evidence-first, no code modified.
> **Sources:** `external-evidence/tinacms/TINACMS_OFFICIAL_FINDINGS.md` (T2 base) + `external-evidence/tinacms-final-verification.md` (6-claim verification). All URLs `https://tina.io/docs/` or `https://tina.io/pricing`.

## TINACMS_TRUTH_MATRIX

| Feature | Official Requirement | Repo Implementation | Status | Evidence |
|---|---|---|---|---|
| Auth — `clientId` | TinaCloud project ID, required; Astro uses `PUBLIC_` prefix | `tina/config.ts:8` `PUBLIC_TINA_CLIENT_ID \|\| TINA_CLIENT_ID \|\| null` | PASS | https://tina.io/docs/tinacloud/deployment-options/cloudflare-workers (Astro "`PUBLIC_TINA_CLIENT_ID`… not Next.js's `NEXT_PUBLIC_`") |
| Tokens — content vs search | Two types: Content (read-only) vs Search (write to search API); canonical envs `TINA_TOKEN` / `TINA_SEARCH_TOKEN` | `tina/config.ts:9` `TINA_TOKEN`; `tina/config.ts:25` `TINA_SEARCH_TOKEN \|\| undefined` | PASS | https://tina.io/docs/reference/config (env table); https://tina.io/docs/tinacloud/dashboard/projects (Tokens Tab) |
| `TINA_READ_ONLY_TOKEN` | No such official name (exact-name site search: zero hits) | Not used in repo | PASS | https://tina.io/docs/reference/config; websearch `site:tina.io "TINA_READ_ONLY_TOKEN"` |
| Build embedding | Admin is statically built SPA; vars embedded at `tinacms build`, not read at runtime | `package.json:18` `tinacms build … && … astro build` | PASS | https://tina.io/docs/reference/config (Build-Time Embedding); Workers doc ("bakes the client ID and token… nothing to add again as runtime variable") |
| Admin deployment | `build.outputFolder: 'admin'` served at `/admin/index.html` alongside site | `tina/config.ts:10-13` `outputFolder: 'admin'`, `publicFolder: 'apps/web/public'` | PASS | https://tina.io/docs/tinacloud/overview (Deploying with admin); https://tina.io/docs/frameworks/astro (admin at `:4321/admin/index.html`) |
| Visual editing — integration | `tina()` in `astro.config.mjs` + SSR adapter (`@astrojs/cloudflare`, `nodejs_compat`) | Per AGENTS.md / `docs/tina-integration.md` architecture (T1 impl. to be verified separately) | WARNING | https://tina.io/docs/contextual-editing/astro (Wire the integration; SSR adapter); Workers doc (`nodejs_compat` for `node:async_hooks`) |
| Visual editing — fields | `tinaField()` stamped as `data-tina-field` on HTML elements; queries wrapped in `requestWithMetadata()` | Same — pattern described in repo docs, per-page wiring unverified here | WARNING | https://tina.io/docs/contextual-editing/astro (click-to-edit); https://tina.io/docs/frameworks/astro (5-item wiring list) |
| Visual editing — island route | One route `src/pages/tina-island/[name].ts` with `export const prerender = false` | Same — must be verified per-file | WARNING | https://tina.io/docs/contextual-editing/astro (Static-site editing requirements) |
| Git-backed save flow | Saves commit to configured GitHub branch → redeploy; `tina-lock.json` must be committed (generated by `tinacms dev`, not build) | `tina/config.ts:4` branch chain; lockfile tracking to be confirmed T1 | WARNING | https://tina.io/docs/tinacloud/overview (Prerequisites); Workers doc (save commits to GitHub → redeploy) |
| Branch behavior | Branch required; Cloudflare resolves via `WORKERS_CI_BRANCH` / `CF_PAGES_BRANCH`, else falls back to `main` (preview would edit `main`) | `tina/config.ts:4` `TINA_BRANCH \|\| GITHUB_BRANCH \|\| WORKERS_CI_BRANCH \|\| CF_PAGES_BRANCH \|\| 'main'` | PASS | https://tina.io/docs/tinacloud/deployment-options/cloudflare-workers (Editing branch) |
| Search | `search.tina.indexerToken` required only when search used; `--skip-search-index[ing]` skips + `tinacms search-index` runs separately | `tina/config.ts:23-30` + `package.json:18` `--skip-search-index`; token intentionally unset | PASS | https://tina.io/docs/reference/search/overview; https://tina.io/docs/cli-overview |
| Free-plan limits | Free: 2 users / limit 2 / 2 roles / 100 MB assets; no Editorial Workflow (starts at Team Plus); saves go direct to protected branch | Repo docs state Free constraints; ops must enforce 2-editor max + app bypass on `main` | PASS with ops condition | https://tina.io/pricing; https://tina.io/docs/drafts/editorial-workflow |

## Area verdicts

- **Auth:** PASS — `tina/config.ts:8-9` matches official model exactly.
- **Tokens:** PASS — correct separation; no phantom `TINA_READ_ONLY_TOKEN`.
- **Build embedding:** PASS — `package.json:18` order correct; Workers Build vars only, no runtime secret.
- **Admin deployment:** PASS — `tina/config.ts:10-13` output/public folders align with `/admin/index.html` SPA model.
- **Visual editing:** WARNING — docs pattern is confirmed SUPPORTED but per-page wiring (`tina()` registration, `TinaIsland`, `tinaField()`, `prerender = false`) is T1 implementation to verify file-by-file, not provable from config alone.
- **Git-backed save flow:** WARNING — model confirmed; `tina/tina-lock.json` tracked-and-committed status and `main` branch-protection bypass for the TinaCloud app need T1 confirmation.
- **Branch behavior:** PASS — `tina/config.ts:4` superset chain covers Workers Builds + Pages correctly.
- **Search:** PASS — disabled by design; do not provision `TINA_SEARCH_TOKEN` unless search is re-enabled.

**No BLOCKERs.** All open claims verified SUPPORTED except `TINA_READ_ONLY_TOKEN`, which is REFUTED (correctly absent from repo).
