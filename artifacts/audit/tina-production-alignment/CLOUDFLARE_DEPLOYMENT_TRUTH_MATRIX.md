# Cloudflare Deployment Truth Matrix — Agent 3 (Workers Specialist)

> **Date:** 2026-09-18 (UTC) | **Source:** `external-evidence/cloudflare-final-verification.md` + `external-evidence/cloudflare/CLOUDFLARE_OFFICIAL_FINDINGS.md` (both `developers.cloudflare.com`-only). No code modified.

| Area | Official Requirement | Repo Implementation | Status | Evidence |
|---|---|---|---|---|
| Builds model | Git-connected two-step: optional build cmd from Root dir → `npx wrangler deploy` (prod) / `npx wrangler versions upload` (preview); API token auto-generated; `name` must match config in Root dir | Root `wrangler.jsonc`; no `wrangler deploy` in CI (`ci.yml:306-312` note); smoke observes post-publish (`ci.yml:292-304`) | **PASS** | `workers/ci-cd/builds/configuration/` (2026-08-28); `wrangler.jsonc:8-16,35` |
| GitHub integration | Connect via "Cloudflare Workers & Pages" GitHub App (install mandatory, no public-repo exemption); org install needs owner/GitHub-Apps-Manager; reinstall on integration errors | Dashboard-connected (wrangler.jsonc:5-6 comment, 2026-08-27); exact "Error fetching GitHub User or Organization details" string not in official docs | **WARNING** | `workers/ci-cd/builds/git-integration/github-integration/` (2026-08-13); error-string meaning UNCLEAR — reinstall app / verify org role dashboard-side |
| Env/secret handling | Build vars/secrets "accessible only to your build… not… at runtime"; runtime via Settings > Variables & Secrets; never put secrets in `vars` | No `vars`/secrets in `wrangler.jsonc`; `TINA_TOKEN` build-only via `ci.yml:30-31`; island needs no runtime secret | **PASS** | `workers/ci-cd/builds/configuration/`; `workers/configuration/secrets/` (2026-07-03); `ci.yml:26-31` |
| KV | `kv_namespaces: [{binding, id, preview_id?}]` (`preview_id` only for `dev --remote`); Astro adapter auto-provisions `SESSION` KV; dashboard-created IDs are not written back to repo | `wrangler.jsonc:45-50` pins `SESSION` / `3435716ffa0e4616b01e2b0faf96ddd5`; no `preview_id` (prod-only use) | **PASS** | `workers/wrangler/configuration/` (2026-09-17); `workers/framework-guides/web-apps/astro/` § Sessions (2026-08-12) |
| Assets+main shape | Worker code + static assets deploy "as a tightly integrated unit"; on-demand rendering requires `main` + `assets` (+ `nodejs_compat`); `404-page` serves nearest `404.html` with 404; default asset-first routing | `wrangler.jsonc:38-43` (`main` → `dist/server/entry.mjs`, `assets` → `dist/client`, `404-page`); one `prerender=false` route (`tina-island/[name].ts:4`), rest static (`astro.config.mjs:13,28`) | **PASS** | `workers/static-assets/` (2026-07-03); `workers/static-assets/binding/` (2026-09-04); Astro guide on-demand template |
| Headers/CSP | `_headers` in asset dir (via `public/`), parsed not served, ≤100 rules; NOT applied to Worker responses; CSP `frame-ancestors` governs iframe embedding (official example uses `'none'`) | `apps/web/public/_headers:1-9` global headers + `frame-ancestors 'self' https://app.tina.io https://*.tinajs.io`; `:11-40` cache matrix; island is JSON-only so Worker-header gap N/A | **PASS** | `workers/static-assets/headers/` (2026-08-25); `apps/web/public/_headers:1-40` |
| Compatibility flags | `compatibility_date` required; `nodejs_compat`(+v2) default-on from `2026-08-04`; explicit flag redundant but ignored/harmless | `wrangler.jsonc:36-37` (`2026-09-14` + `["nodejs_compat"]`) | **PASS** | `workers/configuration/compatibility-flags/` (2026-08-20) |

**Per-area verdicts:** Builds model **PASS** · GitHub integration **WARNING** (dashboard-side: exact error string unverifiable in official docs; reinstall App / confirm org role if "Error fetching GitHub User or Organization details" recurs; no repo change) · Env/secret **PASS** · KV **PASS** · Assets+main **PASS** · Headers/CSP **PASS** · Compat flags **PASS**. No `BLOCKER`. No repo changes required.
