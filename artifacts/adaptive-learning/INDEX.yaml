# Keyword index into ERROR-CATALOG.md — scan this first on any failure.
# Format: <ID>: [symptom keywords...]
index:
  AL-001: [nav overlap, crest overlap, padding-inline, grid overflow, horizontal]
  AL-002: [plain text instead of links, invalid prop, component Props, data shape]
  AL-003: [edit oldString not found, whitespace mismatch]
  AL-004: [powershell, "&&", semicolon, head not recognized, command chaining]
  AL-005: [pnpm.ps1, execution policy, UnauthorizedAccess]
  AL-006: [/opt/pw-browsers, browser executable missing, playwright launch, CI=true]
  AL-007: [slideshow, animation base state, slide covers, opacity]
  AL-008: [scoped CSS no effect, descendant selector, :global, Surface]
  AL-009: [hamburger dead, toggle inert, ClientRouter navigation, stale listeners]
  AL-010: [target-size, 20x24, chevron, hit area, negative margin]
  AL-011: [test asserts old asset, hardcoded path, superseded image]
  AL-012: [unstyled markup, bullets, blue links, stale CSS, hard refresh]
  AL-013: [wrong upload, verify image, stage binary]
  AL-014: [rename, Titans Tigers, entity confirmation]
  AL-015: [deploy claim, needs graph, ci.yml, merge blocking]
  AL-016: [Cloudflare not updated, two deploy paths, live fingerprints]
  AL-017: [spacing void, gap-100, measure rects, rhythm]
  AL-018: [blank capture, reveal, scroll settle, screenshots]
  AL-019: [dev toolbar, capture artifact, viewport-fixed]
  AL-020: [infinite animation test, contract amendment, test exemption]
  AL-021: [setup-node major bump, pnpm not found, Unable to locate executable file, step order]
  AL-022: [axe race in other specs, AxeBuilder grep, fix all harness users]
  AL-023: [black bars capture, sticky fixed smear, stitched screenshot, fullPage clean]
  AL-024: [logo stretch, object-fit fill, brand mark distortion, aspect ratio]
  AL-025: [cover crop tablet, image slot viewport, focal content cut]
  AL-026: [production 404, adapter output moved, wrangler mapping, serve mapping, dist client]
  AL-027: [perf budget message stale, css js global sum, transfer vs raw bytes, tina bridge admin]
  AL-028: [canonical trailing slash, sitemap redirect, 307 platform, served url mismatch]
  AL-029: [lazy hidden image, drawer crest, capture hang, decode wait, eager chrome]
  AL-030: [eager roster grid, squadcard lazy, image requests, below fold grid]
  AL-031: [css budget overrun, scoped duplication, motion keyframes, gradient weight, inline animation]
  AL-032: [trailing slash admin origin, postMessage origin mismatch, isFromAdmin, bridge deaf, TinaCloud edit broken]
  AL-033: [scoped headers CSP, dual content-security-policy, intersection, relaxation no-op, live header count]
  AL-034: [island registration css, duplicated scoped styles, perf budget re-approval, injection fixture]
  AL-035: [excluding not a function, AxeBuilder exclude, playwright api name, installed dist]
  AL-036: [cms editable copy pin, headline fragment rot, content-agnostic marker, object Object]
  AL-037: [static preview server, island route 404, probe skip, live coverage owner]
  AL-038: [runner egress challenge, smoke 403, Just a moment, independent vantage, mitigated]
  AL-039: [gh pr create, powershell quoting, unknown arguments, multi-line body, body-file]
